import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "izs.market.test",
  appName: "izs.market.test",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
